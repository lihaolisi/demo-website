# 两套官网（分别部署）

仓库里是两套互不依赖的静态站点，各自把对应目录作为网站根目录上传即可。

## 1. 河南启力高科

目录：`qili/`

把 `qili` 文件夹内的全部文件部署到站点根路径，入口为 `index.html`。

## 2. 山东云外天信息科技

目录：`yunwaitian/`

把 `yunwaitian` 文件夹内的全部文件部署到站点根路径，入口为 `index.html`。

两套站点资源、页面、样式均已分开，不要混在同一个站点根目录里。

若仍存在旧目录 `jnjt/`，那是拆分前的云外天站点，确认 `yunwaitian/` 无误后可删除。
